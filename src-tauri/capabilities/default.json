{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "Default desktop window capability set for Cobrust Studio.",
  "windows": ["main"],
  "permissions": []
}
